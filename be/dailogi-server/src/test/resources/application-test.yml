spring:
  config:
      import: optional:file:.test-env[.properties]
  datasource:
    url: jdbc:h2:mem:dailogi-db-test
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true

