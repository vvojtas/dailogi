---
import "../styles/global.css";
import Layout from "../layouts/Layout.astro";
import CharacterListPage from "../components/characters/CharacterListPage";
import { ErrorBoundary } from "../components/ErrorBoundary";
---

<Layout title={`Galeria postaci -`}>
  <main class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-8">Galeria Postaci</h1>
    <ErrorBoundary client:only="react">
      <CharacterListPage client:only="react" pageSize={12} />
    </ErrorBoundary>
  </main>
</Layout>
